# TermDesk

# TODO

- config file unter ~/.config/termdesk/config
- diese file auslesen; syntax:

```
header{
    Item(name);
    Item(name);
}
rows{
    row{
        App(name, command, icon);
    }
    row{
        App(name, command, icon);
    }
    row{
        Element(name);
    }
}
```

- daraus wird termdesk gebaut


- letzte verwendete dateien öffnen
- mybe ascii uhr oder so
